"""
4. Stworzyć funkcję, która przyjmie 3 argumenty typu int i sprawdzi czy suma dwóch
pierwszych liczb jest większa lub równa trzeciej, a następnie zwróci tę informację
jako typ logiczny bool

"""

"definicja funkcji porównującej"
def compNumb(x, y, z):

    if (x+y>=z):
        return True
    elif(x+y<z):
        return False
    else:
        return compNumb(x, y, z)

numx = int(input("Wprowadź liczby: "))
numy = int(input("Wprowadź liczby: "))
numz = int(input("Wprowadź liczby: "))
if (compNumb(numx, numy, numz)):
    print(numx, numy, numz, "wynik prawidlowy")
else:
    print(num, "wynik nieprawidlowy")

